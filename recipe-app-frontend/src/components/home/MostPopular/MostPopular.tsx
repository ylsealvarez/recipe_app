import styles from './MostPopular.module.sass'
import Image from 'next/image'

export const MostPopular = () => {
    return (
        <section className={styles.MostPopular}>
            <h2>Most Popular</h2>
        </section>
    )
}